@model IEnumerable<Qinshift.Asp.Net.Mvc.Class04.Views.Models.DtoModels.StudentDto>

<!-- _PartialView.cshtml -->
<div>
    <span>This is a partial view</span>

    <ul>
        @foreach (var item in Model)
        {
            <li>@item.FullName</li>
        }
    </ul>
</div>